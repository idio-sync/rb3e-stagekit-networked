# CircuitPython Settings File
#
# Copy this file to your CIRCUITPY drive and rename it to "settings.toml"
# Then edit the values below with your WiFi credentials.
#
# This file is used by the Pi Pico W to load configuration securely
# without hardcoding credentials in the Python code.

# WiFi Network Credentials
CIRCUITPY_WIFI_SSID = "YOUR_NETWORK_NAME"
CIRCUITPY_WIFI_PASSWORD = "YOUR_NETWORK_PASSWORD"

# Optional: If you want to filter packets from a specific IP only, uncomment below:
# SOURCE_IP_FILTER = "192.168.1.100"
